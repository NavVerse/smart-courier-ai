import React from 'react';
import ReactDOM from 'react-dom/client';
import SmartCourierOnboarding from './SmartCourierOnboarding';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SmartCourierOnboarding />
  </React.StrictMode>
);
